---
# Use OS-aware paths from group_vars, with fallback defaults
isosrc: "{{ deploy_base_path | default('/home/deploy') }}/isosrc"
file_path: "{{ deploy_base_path | default('/home/deploy') }}/baremetal"
mount_path: "{{ mount_base_path | default('/mnt') }}"
